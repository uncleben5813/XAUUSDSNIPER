import { Redis } from '@upstash/redis';
function getRedis(){if(!process.env.KV_REST_API_URL||!process.env.KV_REST_API_TOKEN)throw new Error('KV_REST_API_URL/KV_REST_API_TOKEN missing');return Redis.fromEnv()}
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const sub=typeof req.body==='string'?JSON.parse(req.body):req.body;if(!sub?.endpoint||!sub?.keys?.p256dh||!sub?.keys?.auth)return res.status(400).json({error:'Invalid push subscription'});const r=getRedis(),key='xau_push_subscriptions',list=await r.get(key)||[],next=list.filter(x=>x.endpoint!==sub.endpoint);next.push(sub);await r.set(key,next);return res.status(200).json({ok:true,count:next.length})}catch(e){console.error('push-subscribe',e);return res.status(500).json({error:e.message})}}
