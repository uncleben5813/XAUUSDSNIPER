import { Redis } from '@upstash/redis';
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const redisConfigured=!!(process.env.KV_REST_API_URL&&process.env.KV_REST_API_TOKEN),pub=!!process.env.VAPID_PUBLIC_KEY,priv=!!process.env.VAPID_PRIVATE_KEY,subject=!!process.env.VAPID_SUBJECT;let subscriptionCount=null;if(redisConfigured){const r=Redis.fromEnv(),list=await r.get('xau_push_subscriptions')||[];subscriptionCount=Array.isArray(list)?list.length:0}return res.status(200).json({ok:true,webPush:{vapidPublicKeyConfigured:pub,vapidPrivateKeyConfigured:priv,vapidSubjectConfigured:subject},redis:{configured:redisConfigured,subscriptionCount},ready:pub&&priv&&redisConfigured})}catch(e){return res.status(500).json({ok:false,error:e.message})}}
